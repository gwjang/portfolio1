<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.com.portfolio1.account.Account">
    <select id="select" statementType="CALLABLE" resultType="hashMap" parameterType="hashMap">
        SELECT * FROM TEST;
	</select>
</mapper>
